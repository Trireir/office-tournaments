const about = require('./about-controller');
const addResult = require('./add-result-controller');
const classification = require('./classification-controller');

module.exports = {
  about,
  addResult,
  classification,
};
